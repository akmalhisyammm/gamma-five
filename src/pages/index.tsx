import { Hero } from 'components/home';
import Layout from 'components/layout';

const Home = () => {
  return (
    <Layout title="Beranda">
      <Hero />
    </Layout>
  );
};

export default Home;
